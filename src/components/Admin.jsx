var Admin = React.createClass({
    render: function() {
        return (
            <div id="admin">
                <p>This is the Admin page.</p>
            </div>
        );
    }
});

module.exports = Admin;